# Birthday experience audio

Đặt tệp nhạc đã được cấp quyền sử dụng tại:

```text
public/audio/until-you.mp3
```

Sau khi thêm tệp, đổi `music.isPlaceholder` thành `false` trong:

```text
src/features/experiences/birthday/birthday-experience-config.ts
```

    